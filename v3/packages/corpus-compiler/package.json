{
  "name": "v3-corpus-compiler",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "v3 corpus compiler — surah-parameterized port of v1/packages/corpus-compiler (build-plan step 3). Compiles vendored verse+morphology data into per-surah corpus.json.",
  "scripts": {
    "compile": "node --experimental-strip-types src/compile.ts",
    "validate": "node --experimental-strip-types src/validate.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
