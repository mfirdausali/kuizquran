{
  "name": "v3-fold-runner",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "The sole server-side fold (v3-D08). Pure canonical-order + fold + determinism-compare logic here; a DB adapter for a live deployment is a separate, deferred follow-up (see DECISIONS.md v3-D32).",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "vitest": "^2.1.9"
  }
}
