{
  "name": "worker",
  "private": true,
  "version": "0.5.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply iman-db --local",
    "migrate:remote": "wrangler d1 migrations apply iman-db --remote",
    "test": "vitest run",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.71",
    "@cloudflare/workers-types": "^4.20260702.1",
    "vitest": "^2.1.8",
    "wrangler": "^4.110.0"
  }
}
