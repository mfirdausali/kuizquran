{
  "name": "iman-quiz",
  "private": true,
  "version": "0.1.0",
  "description": "iman.app Yusuf quiz engine — monorepo root",
  "packageManager": "pnpm@10.12.4",
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "tsc --noEmit -p packages/engine/tsconfig.json && tsc --noEmit -p apps/web/tsconfig.json && tsc --noEmit -p packages/corpus-compiler/tsconfig.json && tsc --noEmit -p apps/worker/tsconfig.json"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3"
  }
}
