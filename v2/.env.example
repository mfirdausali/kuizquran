# Frontend (SPA) environment.
#
# HARNESS-02. This file did not exist before; VITE_API_URL was undocumented and
# defaults to http://localhost:8000/api — so a PRODUCTION build with no .env
# silently ships pointing at localhost. Set it for any non-local deploy.

VITE_API_URL=http://localhost:8000/api
