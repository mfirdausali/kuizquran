# Autonomous build progress. The orchestrator (.github/workflows/v2-autobuild.yml)
# reads last_completed to decide the next phase; each phase's PR appends its number here.
# -1 means nothing built yet → next phase is 0.
last_completed=5
# Phase 0: engine ported to v2/src/engine (BUG-3 gap-guard fixed in chain.ts + the
# live rebuild.ts chain_step fold), corpus + iman-ui.css shipped, event log/atoms
# rebuild wired, router added, vitest green (112 tests), dev server verified.
# Phase 1: tap-to-reconstruct engine module (src/engine/src/reconstruct.ts,
# blank count auto-scales Learn 1 -> Reinforce half -> Carry whole-ayah) wired
# through the real /drill screen; new reconstruct_tap/ayah_produced events fold
# into rebuild.ts via the existing S2/S3 grading path (no new RetrievalKind).
# vitest green (125 tests), dev-server-driven Playwright run confirmed strength
# moves and both event types land in the append-only IndexedDB log for both a
# short (12:1, 5 words) and long (12:4, 15 words) ayah.
# Phase 2: daily session loop + pace modes (Steady/Sprint/Maintain, engine/src/
# pace.ts) wired through a real src/session/useSession.ts caller of assembleQueue,
# fixing BUG-1 (real budgetMin) and BUG-2 (real lastActiveDay via activity.ts) live;
# v2-D07 unlock tolerance + recompute-after-in-session-gate-pass; v2-D08 gate
# forgiveness ladder (gateFails, gate_demote event) with a new /gate cold-check
# screen; v2-D11 victory-lap vs weak-seam chains + junction retry. vitest green
# (158 tests incl. a multi-day multi-pace-mode sim and explicit BUG-1/BUG-2
# regressions), dev-server Playwright run confirmed the pace selector changes the
# live queue and a seeded overdue gate surfaces as "make-up" and routes correctly,
# no console errors, no horizontal overflow at 375px.
# Phase 3: onboarding & placement. New /onboarding flow (src/onboarding/) sequences
# returning-hifz binary-search placement (engine/src/placement.ts, already ported,
# now wired to real UI probes) -> anchor hour -> pace mode -> gloss language
# (EN/MS, v2-D27), each persisted locally (session/anchor.ts, session/glossLang.ts,
# placement cache written LAST so a mid-flow exit re-enters at the top - v2-D45).
# Engine gloss resolution parameterized (wordGloss() in corpus.ts; s1Options/
# nextItem/bridgeItems take lang, default "en" - v2-D43). useSession's learn
# window now falls back to the placement start ayah instead of a hardcoded 1
# before anything's encoded (learnWindowStart(), unit tested). Home redirects to
# /onboarding on first run and gained "Not you? switch account" (v2-D12/D44) -
# a confirmed local-only reset (event log + atoms snapshot + all settings) since
# Laravel per-account auth doesn't land until Phase 5. vitest green (186 tests,
# 28 new), dev-server Playwright run confirmed: fresh install -> onboarding ->
# <=5 placement probes -> populated Home with the chosen pace/anchor/lang
# persisted; a carried-placement run correctly starts Home's Learn item at the
# placement's start ayah (not ayah 1); switch-account wipes local state and
# returns to onboarding; no console errors, no horizontal overflow at 375px.
# Phase 4: learner progress surfaces + Test feature. New /progress page (Ring +
# flat Grid, two zoom levels over the same ayahHeatmap data - v2-D24/D25/D48,
# movement table lives app-side in src/progress/movements.ts, engine stays
# surah-agnostic - v2-D48) with a growth curve (heatmap.ts growthCurve()), streak
# calendar (streak.ts completedDayIndices()), average half-life via a reusable
# <InfoTip> (strength.ts halfLifeDays() - v2-D19/D50), Test history, and a raw
# event-log export. New /test page: self-initiated Test (v2-D13-D16) over a
# learner-chosen range (defaults to carried >=80 strength via engine/src/test.ts
# carriedAyat() - v2-D15), mixing all six v2-D13 question types (vocab/cloze/
# junction/locate-the-ayah/produce-from-cold/chaining-reorder as tap-to-order -
# v2-D47), purple accent (v2-D16). Test is read-only BY CONSTRUCTION: rebuild.ts
# has no branch at all for test_start/test_answer/test_result, so they can never
# move strength/due-dates (v2-D46) - proven by a rebuild() before/after equality
# test. Optional "send weak ayat to reviews" nudge records sentToReviews on the
# test_result event; wiring it into assembleQueue is deferred (v2-D49). vitest
# green (212 tests, 26 new), dev-server Playwright run (seeded 10-day event log)
# confirmed: /progress renders the ring (12 arcs), the 111-cell grid, a 6-bar
# growth curve, a 14-day streak calendar, and a working half-life InfoTip; /test
# ran a full range-picker -> mixed-item -> score -> "send to reviews" -> back-to-
# /progress loop with the flag visible in Test history; no console errors, no
# horizontal overflow at 375px.
# Phase 5: Laravel + Sanctum backend at v2/api (bearer personal-access tokens,
# not the stateful cookie guard - v2-D51), anonymous-first auth with claim-in-
# place account adoption (v2-D52: POST /auth/anonymous mints a device row,
# /auth/register claims it in place, /auth/login switches to an existing
# account's token). users/events migrations reproduce v1's D1 schema plus the
# v2 Phase 1-4 event fields; events.id is the client uuid PK itself, idempotent
# via insertOrIgnore (v2-D53). New src/sync/{auth,outbox,useBackgroundSync}.ts
# on the client (Bearer auth replaces v1's session cookie in the ported
# outbox.ts), mounted once at the app root, plus a minimal Account panel on
# Home for email-based save/sign-in (social deferred). vitest green (232 tests,
# 20 new) and `php artisan test` green (20 tests). Live end-to-end curl run
# confirmed: anonymous device posts events -> idempotent resend is a no-op ->
# register adopts the device -> a second device logs in and GET /events
# hydrates the identical event history.
